---
applyTo: "dev/hec/config/**"
---

# Configuration and secret safety instructions

Treat configuration and secret-handling changes as LARGE changes. Keep secrets
out of tracked files and logs, preserve public-config redaction, and keep
`controller.enabled=false` and `tng.write_enabled=false` in local development
and preview profiles.